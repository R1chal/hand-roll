# 手写 Nginx 配置文件
# 模拟 Nginx 配置格式

# 监听端口
listen 8080;

# 服务器名称
server_name localhost;

# 根目录
root /var/www/html;

# location 配置
location / {
    root /var/www/html;
}

location /api {
    proxy_pass http://localhost:9090;
}

location /static {
    alias /var/www/static;
}
